import React from 'react';
import Navigation from '../../Navigation/Navigation';

const Header = (props) => (
    <header>
        <h1>Brandon Lenz</h1>
        <h2>Engineer</h2>
        <Navigation/>
    </header>
);

export default Header;
